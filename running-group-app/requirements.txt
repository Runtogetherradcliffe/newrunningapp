# Core dependencies
pandas>=2.0.0
requests>=2.28.0

# Google APIs (for Calendar sync and OAuth)
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.0

# Web UI (Streamlit)
streamlit>=1.28.0

# Optional: Strava route enrichment
# (API calls use requests, no additional deps)
